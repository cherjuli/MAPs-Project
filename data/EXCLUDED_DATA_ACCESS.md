# Excluded data access notes

## Raw SEC 10-K filing text
Not committed due to redistribution/volume constraints. Recreate by running the collection workflow (planned under `code/01_data_collection/`) against SEC EDGAR and storing raw files locally in `data/raw/`.

## LSEG Data & Analytics inputs
Not committed because the source is proprietary and subscription-gated. Obtain through your institutional LSEG access and store locally in a non-committed location (for example `data/proprietary/`).

## Annotated evaluation dataset with filing excerpts
Not committed when annotation files contain substantial reproduced filing text. Share only schema/prompt templates publicly and provide access under separate permissions where legally allowed.
